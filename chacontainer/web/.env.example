# Copy to .env.local for `npm run dev`. Leave unset (or empty) when serving
# through the docker-compose nginx proxy - it already forwards /api to the
# api container on the same origin, so no absolute URL is needed there.
VITE_API_URL=http://localhost:8081
